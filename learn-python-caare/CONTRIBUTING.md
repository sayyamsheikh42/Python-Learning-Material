# Contributing

1. Fork the repo and create a feature branch.
2. For each module, keep structure:
   - `README.md` (concepts, visuals to run, exercises)
   - `examples.py` (runnable code examples)
   - `visuals.py` (plots using matplotlib — no seaborn/styles)
   - `exercises/` and `solutions/`
3. Add tests if you introduce complex code.
4. Use clear, beginner-friendly comments.
